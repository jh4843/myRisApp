<UserControl x:Class="myRis.Web.Scp.Setting.View.ToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style BasedOn="{StaticResource myFlatButtonStyle}" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="33" />
            <Setter Property="Margin" Value="1,0" />
        </Style>
    </UserControl.Resources>
    <DockPanel Background="#ffffff">
        <StackPanel
            x:Name="stkpToolBtnList"
            Margin="5"
            HorizontalAlignment="Left"
            DockPanel.Dock="Top"
            Orientation="Horizontal">
            <Button x:Name="btnMwlList"
                    Grid.Column="1"
                    Content="     MWL     "
                    ToolTip="Modality worklist"/>
        </StackPanel>
    </DockPanel>
</UserControl>